[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "msft-valuation-model"
version = "0.1.0"
description = "Probabilistic Microsoft valuation model"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

